import { Card, CardHeader, CardTitle, CardContent } from '../../components/ui/Card';

export default function ProductsModerationPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Products Moderation</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-600">Moderate platform products</p>
      </CardContent>
    </Card>
  );
}
