[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["cd frontend && npm install --legacy-peer-deps && npm run build"]

[phases.start]
cmd = "node server.js"
